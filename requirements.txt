torch>=2.1.0
transformers>=4.38.0
peft>=0.8.0
accelerate>=0.27.0
datasets>=2.17.0
wandb>=0.16.0
numpy>=1.24
pillow>=10.0
pyyaml>=6.0
tqdm>=4.65
matplotlib>=3.7
einops>=0.7
# Isaac Gym is installed separately from NVIDIA — Linux + NVIDIA GPU only.
# isaacgym
